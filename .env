# 👇 BẮT BUỘC: dùng chung giữa auth, admin, gateway
JWT_SECRET=devsecret_change_me

# (Tùy chọn) Gateway session secret
GATEWAY_SECRET=dev_gateway_secret_change_me

# Dùng SQLite bền vững cho auth-service (volume /data)
AUTH_DATABASE_URL=sqlite:////data/auth.db

# Gateway gọi nội bộ các service theo tên (trong network docker)
AUTH_URL_INTERNAL=http://auth_service:5001
ADMIN_URL_INTERNAL=http://admin_service:5003
