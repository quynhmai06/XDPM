
JWT_SECRET=dev_jwt_secret_change_me

# Gateway secret used by gateway service; optional default for local dev
GATEWAY_SECRET=dev_gateway_secret

# External provider settings for pricing-service (set PROVIDER to e.g. openai, gemini, none)
PROVIDER=none
GOOGLE_API_KEY=
OPENAI_API_KEY=
GEMINI_MODEL=
OPENAI_MODEL=

# Optional timeouts / cache for pricing-service
SOFT_TIMEOUT=8
HARD_TIMEOUT=15
CACHE_TTL=600

# Optional override URLs (use internal docker-compose service names for inter-service comms)
AUTH_URL_INTERNAL=http://auth_service:5001
ADMIN_URL_INTERNAL=http://admin_service:5003

# Database connection examples (uncomment and adapt if needed)
# For sqlite (auth service local dev)
# DATABASE_URL=sqlite:////data/auth.db
# For postgres (services using postgres)
# DATABASE_URL=postgresql+psycopg2://ev:evpass@postgres:5432/evdb
JWT_SECRET=supersecret
GATEWAY_SECRET=dev_gateway_secret
AUTH_URL_INTERNAL=http://auth_service:5001
ADMIN_URL_INTERNAL=http://admin_service:5003
LISTING_URL=http://listing_service:5002
PRICING_URL=http://pricing_service:5003
FAVORITES_URL=http://favorites_service:5004

PROVIDER=auto
GOOGLE_API_KEY=
GEMINI_MODEL=models/gemini-2.5-flash
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
SOFT_TIMEOUT=12
HARD_TIMEOUT=30
CACHE_TTL=600
STRICT_AI=0

# Bank details used for QR generation (change for production)
BANK_NAME=MB Bank
BANK_ACCOUNT=0359506148
BANK_OWNER=Second-hand EV & Battery Trading Platform
